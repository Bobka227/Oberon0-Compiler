module err_for;
var x: real;
begin
  (* не объявлен i *)
  for i := 0 to 10 do writeln end;

  (* индекс real *)
  for x := 0 to 3 do writeln end;

  (* границы — не integer *)
  var j: integer;  (* локальных глобально нельзя, оставим без неё: просто ещё один модуль ниже *)
end err_for.

